<UserControl x:Class="BMES.Modules.Genealogy.Views.GenealogyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <StackPanel Margin="10">
            <TextBlock Text="Product Genealogy" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="Product ID:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBox Text="{Binding ProductId, UpdateSourceTrigger=PropertyChanged}" Width="100"/>
                <Button Content="Show Genealogy" Command="{Binding ShowGenealogyCommand}" Margin="10,0,0,0"/>
            </StackPanel>
            <TextBlock Text="{Binding GenealogyTree}" TextWrapping="Wrap" Margin="0,10,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>