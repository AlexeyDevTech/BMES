<Window x:Class="BMES.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:BMES"
        xmlns:Regions="clr-namespace:BMES.Regions;assembly=BMES.Regions"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
        Title="{Binding Title}" Height="850" Width="1525" >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" x:Name="TopBoard">
            
        </Grid>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="50"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0" x:Name="LeftBoard">
                
            </Grid>
            <Grid Grid.Column="1">
                <ContentControl prism:RegionManager.RegionName="{x:Static Regions:RegionNames.ContentRegion}" />
            </Grid>
        </Grid>
    </Grid>
</Window>
