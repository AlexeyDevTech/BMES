<UserControl
    x:Class="BMES.Modules.Alarms.Views.AlarmsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:prism="http://prismlibrary.com/"
    prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding Alarms}">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Timestamp}" Header="Timestamp" />
                <DataGridTextColumn Binding="{Binding Message}" Header="Message" />
                <DataGridTextColumn Binding="{Binding Severity}" Header="Severity" />
                <DataGridCheckBoxColumn Binding="{Binding IsActive}" Header="Active" />
                <DataGridCheckBoxColumn Binding="{Binding IsAcknowledged}" Header="Acknowledged" />
                <DataGridTemplateColumn>
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button
                                Command="{Binding DataContext.AcknowledgeAlarmCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                CommandParameter="{Binding}"
                                Content="Acknowledge" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
