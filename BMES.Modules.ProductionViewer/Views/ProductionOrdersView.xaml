<UserControl x:Class="BMES.Modules.ProductionViewer.Views.ProductionOrdersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:models="clr-namespace:BMES.Core.Models;assembly=BMES.Core"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Production Orders" FontSize="20" FontWeight="Bold" Margin="10"/>
        <DataGrid Grid.Row="1" ItemsSource="{Binding ProductionOrders}" SelectedItem="{Binding SelectedProductionOrder}" AutoGenerateColumns="True" IsReadOnly="True" Margin="10">
        </DataGrid>
    </Grid>
</UserControl>
